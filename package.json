{
  "name": "sgsmm",
  "version": "0.1.0",
  "private": true,
  "description": "Sortino-Gated Smart Money Mirror — Manager Scoring Infrastructure for Mantle Eco Fund",
  "license": "MIT",
  "packageManager": "pnpm@10.21.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "pnpm": {
    "overrides": {
      "@hono/node-server": ">=1.19.13"
    }
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r --if-present test && echo 'NOTE: contracts → forge test | backtest/agent → pytest in their venvs'",
    "lint": "pnpm -r --if-present lint",
    "indexer:dev": "pnpm --filter @sgsmm/indexer dev",
    "frontend:dev": "pnpm --filter @sgsmm/frontend dev"
  }
}
